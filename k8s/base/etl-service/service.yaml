apiVersion: v1
kind: Service
metadata:
  name: etl-service
  namespace: default
  labels:
    app: etl-service
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: etl-service
