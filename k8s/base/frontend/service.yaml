apiVersion: v1
kind: Service
metadata:
  name: frontend
  labels:
    app: frontend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080  # Changed to match non-root nginx port
    protocol: TCP
    name: http
  selector:
    app: frontend
